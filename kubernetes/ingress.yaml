apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-web-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: hello.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-web-service
                port:
                  number: 80